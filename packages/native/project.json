{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "native",
  "projectType": "library",
  "sourceRoot": "packages/native/src",
  "targets": {
    "build": {
      "command": "tsc -p {projectRoot}/tsconfig.json"
    },
    "test": {
      "command": "jest --config {projectRoot}/jest.config.mjs"
    },
    "nx-release-publish": {
      "dependsOn": ["build", "^nx-release-publish"]
    }
  }
}
